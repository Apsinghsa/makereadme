# Copied to the VPS as deploy/prod.env and consumed by the deploy job.
# Fill these in the repo's GitHub → Settings → Secrets.

# MAKEREADME runtime
GEMINI_API_KEY=<your-gemini-key>
GITHUB_TOKEN=<token-with-repo-scope-to-generate-readmes>
PORT=5000